export type IReturnTo = {
  reqUrl: string;
  reqQueryParams: string;
};

export type ILeftNav = {
  body: any;
  header: string;
  radioBtn?: boolean;
};

export type IDropdownOptions = {
  label: string;
  value: string | number;
};
