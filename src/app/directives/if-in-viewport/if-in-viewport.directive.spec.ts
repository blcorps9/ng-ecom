import { IfInViewportDirective } from './if-in-viewport.directive';

describe('IfInViewportDirective', () => {
  it('should create an instance', () => {
    const directive = new IfInViewportDirective();
    expect(directive).toBeTruthy();
  });
});
