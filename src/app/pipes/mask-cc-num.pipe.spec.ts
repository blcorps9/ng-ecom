import { MaskCcNumPipe } from './mask-cc-num.pipe';

describe('MaskCcNumPipe', () => {
  it('create an instance', () => {
    const pipe = new MaskCcNumPipe();
    expect(pipe).toBeTruthy();
  });
});
